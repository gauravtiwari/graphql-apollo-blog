import { SelectionSet } from 'graphql';
import { NormalizedCache } from './store';
export declare function readQueryFromStore({store, query, variables}: {
    store: NormalizedCache;
    query: string;
    variables?: Object;
}): Object;
export declare function readFragmentFromStore({store, fragment, rootId, variables}: {
    store: NormalizedCache;
    fragment: string;
    rootId: string;
    variables?: Object;
}): Object;
export declare function readSelectionSetFromStore({store, rootId, selectionSet, variables}: {
    store: NormalizedCache;
    rootId: string;
    selectionSet: SelectionSet;
    variables: Object;
}): Object;
