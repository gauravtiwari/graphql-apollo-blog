
<%= react_component('PostsShowComponent', {post: {
    id: @post.id,
    title: @post.title,
    body: @post.body
  }}.to_json, {tag: "article", prerender: true})
%>

<%= link_to('Go Back', root_path) %>

